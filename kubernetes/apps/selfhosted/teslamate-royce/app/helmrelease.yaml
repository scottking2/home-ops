---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: teslamate-royce
  namespace: selfhosted
spec:
  interval: 30m
  chart:
    spec:
      chart: app-template
      version: 4.6.2
      sourceRef:
        kind: HelmRepository
        name: bjw-s
        namespace: flux-system
      interval: 30m

  install:
    createNamespace: false
    remediation:
      retries: 5
  upgrade:
    cleanupOnFail: true
    remediation:
      retries: 5
  uninstall:
    keepHistory: false

  values:
    controllers:
      teslamate-royce:
        strategy: Recreate
        annotations:
          reloader.stakater.com/auto: "true"

        pod:
          enableServiceLinks: false

        initContainers:
          init-db:
            image:
              repository: ghcr.io/home-operations/postgres-init
              tag: "18.3"
              pullPolicy: IfNotPresent
            env:
              INIT_POSTGRES_SUPER: "true"
            envFrom:
              - secretRef:
                  name: teslamate-royce-init-db

        containers:
          app:
            image:
              repository: teslamate/teslamate
              tag: "3.0.0"
              pullPolicy: IfNotPresent

            env:
              TZ: America/Denver
              DISABLE_MQTT: "false"
              MQTT_HOST: mosquitto.home-automation.svc.cluster.local
              MQTT_PORT: "1883"
              MQTT_TLS: "false"
              MQTT_NAMESPACE: royce

            envFrom:
              - secretRef:
                  name: teslamate-royce-secret

            resources:
              requests:
                cpu: 50m
                memory: 250Mi
              limits:
                memory: 512Mi

    service:
      teslamate-royce:
        controller: teslamate-royce
        ports:
          http:
            port: 4000

    ingress:
      internal:
        enabled: true
        className: internal
        annotations:
          external-dns.alpha.kubernetes.io/target: "internal.${SECRET_DOMAIN}"
        hosts:
          - host: "teslamate-royce.${SECRET_DOMAIN}"
            paths:
              - path: /
                pathType: Prefix
                service:
                  identifier: teslamate-royce
                  port: http
        tls:
          - hosts:
              - "teslamate-royce.${SECRET_DOMAIN}"
      external:
        enabled: true
        className: external
        annotations:
          external-dns.alpha.kubernetes.io/target: "external.${SECRET_DOMAIN}"
        hosts:
          - host: "teslamate-royce.${SECRET_DOMAIN}"
            paths:
              - path: /
                pathType: Prefix
                service:
                  identifier: teslamate-royce
                  port: http
        tls:
          - hosts:
              - "teslamate-royce.${SECRET_DOMAIN}"
